package com.example.projectgroup1.repository;

import com.example.projectgroup1.entity.TourLocations;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TourLocationsRepository extends JpaRepository<TourLocations, Integer> {
}
